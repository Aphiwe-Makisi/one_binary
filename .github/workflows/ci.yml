name: Go CI & Release

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:

  # --------------------------------------------------
  # 1. Code Quality: gofmt
  # --------------------------------------------------
  gofmt:
    name: 📝 Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 1.23
      - name: Check formatting
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "❌ Code not formatted:"
            gofmt -l .
            exit 1
          fi

  # --------------------------------------------------
  # 2. Go Vet
  # --------------------------------------------------
  go-vet:
    name: 🔍 Go Vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 1.23
      - run: go vet ./...

  # --------------------------------------------------
  # 3. Lint
  # --------------------------------------------------
  lint:
    name: 🧹 Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 1.23
      - uses: golangci/golangci-lint-action@v6
        with:
          version: latest

  # --------------------------------------------------
  # 4. Unit Tests + Coverage
  # --------------------------------------------------
  tests:
    name: 🧪 Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 1.23

      # Caches Go dependencies (super fast!)
      - name: Cache Go Modules
        uses: actions/cache@v4
        with:
          path: ~/.cache/go-build
          key: go-build-${{ hashFiles('go.sum') }}

      - run: go mod download

      - name: Run tests
        run: go test ./... -race -coverprofile=coverage.out

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: coverage.out

  # --------------------------------------------------
  # 5. Security Scan
  # --------------------------------------------------
  security:
    name: 🔒 Security Scan
    needs: tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: securego/gosec@master
        with:
          args: ./...

  # --------------------------------------------------
  # 6. Build Static Binaries (multi-arch)
  # --------------------------------------------------
  build:
    name: 🔨 Build Static Binaries
    needs: [gofmt, go-vet, lint, tests]
    runs-on: ubuntu-latest

    strategy:
      matrix:
        goos: [linux]
        goarch: [amd64, arm64]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 1.23

      - name: Cache Go Modules
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: go-mod-${{ hashFiles('go.sum') }}

      - run: go mod download

      - name: Build static binary
        run: |
          CGO_ENABLED=0 \
          GOOS=${{ matrix.goos }} \
          GOARCH=${{ matrix.goarch }} \
          go build -tags netgo -ldflags="-s -w -extldflags=-static" \
          -o goscripts-${{ matrix.goos }}-${{ matrix.goarch }} \
          ./cmd/goscripts

      - uses: actions/upload-artifact@v4
        with:
          name: goscripts-${{ matrix.goos }}-${{ matrix.goarch }}
          path: goscripts-${{ matrix.goos }}-${{ matrix.goarch }}
