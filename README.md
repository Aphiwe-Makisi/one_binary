# go-scripts

A monorepo for small Go utility scripts built into a single CLI binary.

## Features
- Single binary with multiple subcommands (scripts)
- Global YAML config + env overrides (via viper)
- Cobra-based CLI
- GitHub Actions CI (tests + build + artifact)